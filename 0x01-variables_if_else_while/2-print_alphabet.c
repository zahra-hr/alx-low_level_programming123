#include <stdio.h>

/**
 * main - Entry point
 * Description: A C program to display Alphabet
 * Return: 0 (success)
*/

int main(void)
{
	for (char i = 'a'; i < 'z'; i++)
		putchar(i);
	return (0);
}
