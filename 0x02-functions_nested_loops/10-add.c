#include "main.h"

/**
 * add -  function that adds two integers
 * @x: first parameter
 * @y: second parameter
 * Return: the sum x + y
*/
int add(int x, int y)
{
	int sum = x + y;

	return (sum);
}
